module Backend.ZeroCreate

open GraphBLAS.FSharp.Backend
open GraphBLAS.FSharp.Tests

(*let checkResult actual (expected: 'a []) =
    match actual with
    | VectorCOO actual ->
        let expected = createVectorFromArray expected
        ()

    | VectorDense actual ->
        ()*)


